import { LoginForm } from "@/components/LoginForm";
import React from "react";

const Page = () => {
  return (
    <section>
      <main>
        <LoginForm />
      </main>
    </section>
  );
};

export default Page;
